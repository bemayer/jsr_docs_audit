{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "lldb",
      "request": "launch",
      "name": "Debug audit_script",
      "cargo": {
        "args": ["build", "-p", "audit_script", "--bin=cli"],
        "filter": {
          "name": "cli",
          "kind": "bin"
        }
      },
      "args": ["@bemayer/rubique"],
      "cwd": "${workspaceFolder}"
    }
  ]
}
